const button=document.querySelector('.menu-button');const nav=document.querySelector('.main-nav');button?.addEventListener('click',()=>{const open=nav.classList.toggle('open');button.setAttribute('aria-expanded',String(open));});nav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');button?.setAttribute('aria-expanded','false');}));const toast=document.querySelector('.contact-toast');let timer;document.querySelectorAll('[data-contact]').forEach(item=>item.addEventListener('click',()=>{toast?.classList.add('show');clearTimeout(timer);timer=setTimeout(()=>toast?.classList.remove('show'),2600);}));